---
'marking-menu': minor
---

Show a dot growing inside a circle during the pause before a menu or submenu opens. It fades in instead when the user prefers reduced motion. Style it with `--mm-indicator-fill` and `--mm-indicator-background`.
