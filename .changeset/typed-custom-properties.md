---
'marking-menu': minor
---

pr: #382
commit: fc6b7c5

Change the default colors: wedges, plates, and connectors share a gray fill with white labels. Set `--mm-fill` and `--mm-fill-active` to recolor them together. Colors follow dark mode when the page declares `color-scheme: light dark`.
