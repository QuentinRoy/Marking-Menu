---
'marking-menu': minor
---

`rxjs` is gone: not a dependency, not a peer dependency, not a runtime
import. Events reach you through the controller's plain `on`/`dispose` API
with nothing to install or pin a compatible version of.
