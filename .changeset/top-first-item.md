---
'marking-menu': major
---

Default item positions now start at the top instead of the right: an item without a stated `angle` is placed 90 degrees counterclockwise from its previous default. A learned gesture can now select a different item, without any error. Set each item's `angle` to keep its old direction.
