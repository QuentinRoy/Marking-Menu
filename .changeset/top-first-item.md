---
'marking-menu': major
---

Default item positions now start at the top instead of the right: a menu level where no item states an `angle` spreads its items 90 degrees counterclockwise from the previous default. Levels with at least one stated angle are unaffected. A learned gesture can now select a different item, without any error. Set each item's `angle` to keep its old direction.
